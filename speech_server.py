# Speech server for Render deployment
import os
import tempfile

def transcribe_audio(audio_path, language="en"):
    """Placeholder transcription function for Render deployment"""
    return "Voice transcription temporarily disabled on free hosting"